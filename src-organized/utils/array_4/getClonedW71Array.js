/**
 * Returns a shallow copy of the W71 array.
 *
 * @returns {Array<any>} a new array containing the elements of W71.
 */
function getClonedW71Array() {
  // Create and return a shallow copy of the W71 array using the spread operator
  return [...W71];
}

module.exports = getClonedW71Array;