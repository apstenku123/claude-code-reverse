/**
 * Checks if the hardware feature is enabled.
 *
 * @returns {boolean} Returns false indicating the hardware feature is disabled.
 */
function isHardwareEnabled() {
  // Always returns false as hardware is not enabled
  return false;
}

module.exports = isHardwareEnabled;