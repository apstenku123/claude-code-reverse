/**
 * Checks if the hardware is available.
 *
 * @returns {boolean} Returns false to indicate hardware is not available.
 */
function isHardwareAvailable() {
  // Always returns false, indicating hardware is not available
  return false;
}

module.exports = isHardwareAvailable;