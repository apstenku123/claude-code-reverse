/**
 * Returns the name of the production environment.
 *
 * @returns {string} The string 'production', representing the production environment name.
 */
function getProductionEnvironmentName() {
  // Return the environment name as a string
  return "production";
}

module.exports = getProductionEnvironmentName;