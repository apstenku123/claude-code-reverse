/**
 * Generates a string consisting of a repeated delimiter and optional separator, ending with a final suffix if the count is non-zero.
 *
 * @param {number} count - The number of delimiter segments to generate.
 * @returns {string} The constructed string with delimiters and optional suffix.
 */
function generateDelimitedString(count) {
  let result = "";
  // Append the delimiter and separator for each segment except the last
  for (let index = 0; index < count; index++) {
    result += k30; // Append the delimiter
    // If not the last segment, append the separator generated by generateJ5AString
    if (index < count - 1) {
      result += generateJ5AString();
    }
  }
  // If at least one segment was added, append the final suffix
  if (count) {
    result += j30;
  }
  return result;
}

module.exports = generateDelimitedString;