/**
 * Retrieves the 'NEVER' constant from the KNA object.
 *
 * @returns {string} The value of KNA.NEVER constant.
 */
function getNeverKNAValue() {
  // Return the 'NEVER' constant from the KNA object
  return KNA.NEVER;
}

module.exports = getNeverKNAValue;