/**
 * Retrieves the NOOP_METER constant from the nZ0 module.
 *
 * This accessor function provides a standardized way to obtain the NOOP_METER value,
 * which is typically used as a no-operation placeholder for meter-related operations.
 *
 * @returns {*} The NOOP_METER constant from the nZ0 module.
 */
function getNoopMeter() {
  // Return the no-operation meter constant from the nZ0 module
  return nZ0.NOOP_METER;
}

module.exports = getNoopMeter;