/**
 * Retrieves the current year unit value.
 *
 * @returns {string} The current year unit value.
 */
function getYearUnit() {
  // Return the value of the year unit from external context
  return yearUnit;
}

module.exports = getYearUnit;