/**
 * Sets the global variable 'globalValue' to the provided value.
 *
 * @param {*} value - The value to assign to the global variable.
 * @returns {void}
 */
function setGlobalValue(value) {
  // Assign the provided value to the global variable
  globalValue = value;
}

module.exports = setGlobalValue;