/**
 * Retrieves the total cost from the N9 object.
 *
 * @returns {number} The total cost value stored in N9.totalCost.
 */
function getTotalCost() {
  // Access and return the total cost from the N9 object
  return N9.totalCost;
}

module.exports = getTotalCost;