/**
 * Checks if the currentValue is strictly equal to the expectedValue.
 *
 * @returns {boolean} True if currentValue equals expectedValue, false otherwise.
 */
const areValuesEqual = () => {
  // Compare the current value with the expected value
  return currentValue === expectedValue;
};

module.exports = areValuesEqual;