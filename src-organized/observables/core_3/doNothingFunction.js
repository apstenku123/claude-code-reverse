/**
 * Does nothing and returns undefined.
 *
 * @function doNothingFunction
 * @param {*} sourceObservable - The source observable or input value (unused).
 * @param {*} config - Configuration object or parameter (unused).
 * @returns {undefined} Always returns undefined.
 */
function doNothingFunction(sourceObservable, config) {
  // This function intentionally does nothing and returns undefined
  return;
}

module.exports = doNothingFunction;