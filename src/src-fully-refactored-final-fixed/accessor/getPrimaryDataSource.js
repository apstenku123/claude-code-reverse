/**
 * Retrieves the primary data source used by the application.
 *
 * @returns {*} The primary data source object or value.
 */
function getPrimaryDataSource() {
  // Return the main data source reference (imported or defined elsewhere)
  return EE1;
}

module.exports = getPrimaryDataSource;