/**
 * Creates an accessor object with a 'not' property.
 *
 * This function is typically used as a placeholder or stub accessor,
 * returning an object with a single 'not' property, which is an empty object.
 *
 * @returns {Object} An accessor object containing a 'not' property.
 */
const createNotAccessor = () => {
  // Return an object with a 'not' property as an empty object
  return {
    not: {}
  };
};

module.exports = createNotAccessor;