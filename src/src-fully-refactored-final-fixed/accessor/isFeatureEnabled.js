/**
 * Checks if a specific feature is enabled.
 *
 * @returns {boolean} Returns false, indicating the feature is disabled.
 */
function isFeatureEnabled() {
  // This accessor always returns false, indicating the feature is not enabled.
  return false;
}

module.exports = isFeatureEnabled;