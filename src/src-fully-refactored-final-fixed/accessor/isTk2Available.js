/**
 * Checks if the Tk2 feature is available.
 *
 * @returns {boolean} Returns false, indicating that the Tk2 feature is not available.
 */
function isTk2Available() {
  // Tk2 feature is currently not available, always return false
  return false;
}

module.exports = isTk2Available;