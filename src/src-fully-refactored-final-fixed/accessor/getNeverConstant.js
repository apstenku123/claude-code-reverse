/**
 * Retrieves the 'NEVER' constant from the KNA module.
 *
 * @returns {string} The 'NEVER' constant value defined in KNA.
 */
function getNeverConstant() {
  // Return the 'NEVER' constant from the KNA module
  return KNA.NEVER;
}

module.exports = getNeverConstant;