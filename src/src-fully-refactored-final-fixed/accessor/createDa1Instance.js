/**
 * Creates and returns a new instance of the Da1 class.
 *
 * @returns {Da1} a new instance of the Da1 class.
 */
function createDa1Instance() {
  // Instantiate and return a new Da1 object
  return new Da1();
}

module.exports = createDa1Instance;