/**
 * Checks if the YD6 feature is enabled.
 *
 * @returns {boolean} Returns false, indicating the YD6 feature is always disabled.
 */
function isYD6Enabled() {
  // The YD6 feature is currently always disabled.
  return false;
}

module.exports = isYD6Enabled;