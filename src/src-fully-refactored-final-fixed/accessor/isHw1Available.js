/**
 * Checks if the HW1 feature is available.
 *
 * @returns {boolean} Returns false, indicating that the HW1 feature is not available.
 */
function isHw1Available() {
  // HW1 feature is currently not available
  return false;
}

module.exports = isHw1Available;