/**
 * a no-op accessor function that accepts an input but does nothing and returns undefined.
 *
 * @param {*} inputValue - The input value (unused).
 * @returns {undefined} Always returns undefined.
 */
const noopAccessor = (inputValue) => {
  // This function intentionally does nothing
};

module.exports = noopAccessor;
