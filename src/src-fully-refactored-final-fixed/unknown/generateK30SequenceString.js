/**
 * Generates a string consisting of a repeated pattern based on the provided count.
 * Each repetition appends the value of the global variable `k30`, and, except for the last repetition, also appends a string generated by `generateJ5AppendedString()`.
 * If the count is greater than zero, the global variable `j30` is appended at the end.
 *
 * @param {number} count - The number of times to repeat the pattern.
 * @returns {string} The generated string sequence.
 */
function generateK30SequenceString(count) {
  let resultString = "";

  // Loop to build the repeated pattern
  for (let index = 0; index < count; index++) {
    // Append k30 and, unless isBlobOrFileLikeObject'createInteractionAccessor the last iteration, append the generated string
    resultString += k30 + (index < count - 1 ? generateJ5AppendedString() : "");
  }

  // If count is greater than zero, append j30 at the end
  if (count) {
    resultString += j30;
  }

  return resultString;
}

module.exports = generateK30SequenceString;