/**
 * Retrieves the value of the global variable U_.
 *
 * @returns {*} The current value of the global U_ variable.
 */
const getGlobalUValue = () => {
  // Return the global U_ variable as is
  return U_;
};

module.exports = getGlobalUValue;
