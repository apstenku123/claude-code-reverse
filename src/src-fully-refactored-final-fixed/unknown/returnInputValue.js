/**
 * Returns the input value as-is without any modification.
 *
 * @param {any} inputValue - The value to be returned.
 * @returns {any} The same value that was passed as input.
 */
function returnInputValue(inputValue) {
  // No transformation or processing; simply return the input
  return inputValue;
}

module.exports = returnInputValue;