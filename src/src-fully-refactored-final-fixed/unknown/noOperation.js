/**
 * @function noOperation
 * @description
 * This function performs no operation and returns undefined. It is typically used as a placeholder or default callback.
 *
 * @returns {undefined} No value is returned.
 */
function noOperation() {
  // Intentionally left blank - this is a no-op function
}

module.exports = noOperation;