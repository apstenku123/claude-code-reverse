/**
 * Throws the provided error or exception.
 *
 * @param {any} error - The error or exception to be thrown.
 * @throws {any} Throws the provided error.
 */
function throwError(error) {
  // Immediately throw the provided error or exception
  throw error;
}

module.exports = throwError;