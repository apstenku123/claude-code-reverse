/**
 * Returns a string indicating the status of an operation.
 *
 * @returns {string} The string 'success' indicating the operation was successful.
 */
function getOperationStatus() {
  // Always returns the status string 'success'
  return "success";
}

module.exports = getOperationStatus;