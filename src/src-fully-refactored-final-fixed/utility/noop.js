/**
 * a no-operation utility function that performs no action.
 *
 * This function can be used as a default callback or placeholder where a function is required but no operation is needed.
 *
 * @returns {void} Does not return a value.
 */
function noop() {}

module.exports = noop;