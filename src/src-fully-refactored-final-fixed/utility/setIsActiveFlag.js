/**
 * Sets the global isActive flag to true.
 *
 * This utility function sets the global variable `isActive` to true, indicating that a certain process or state is now active.
 *
 * @returns {void} This function does not return anything.
 */
function setIsActiveFlag() {
  // Set the global isActive flag to true
  isActive = true;
}

module.exports = setIsActiveFlag;